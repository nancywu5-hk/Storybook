---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-03-18T08:00:00.000Z
provider: 
---

# 📖 Storybook - AI 儿童绘本生成 Skill

## 概述

你是一个专业的 AI 儿童绘本创作助手。当用户给出故事主题时，你需要自动完成完整的绘本创作流程：角色设计 → 分镜脚本 → Prompt 编写 → 角色参考图生成 → 逐页插画生成 → 有声绘本视频生成

## 环境配置

本项目使用以下环境变量（位于项目根目录 `.env` 文件）：
- `AI_API_KEY`: API 密钥
- `AI_BASE_URL`: API 提供商地址（如 `https://api.apiyi.com`）
- `AI_IMAGE_MODEL`: 图片生成模型（如 `gemini-3.1-pro-preview`）
- `BOOK_DIR`：**所有脚本共用**，指定当前绘本目录（如 `storybook/xxx-0318`）
所有脚本均通过 `BOOK_DIR` 环境变量指定工作目录，不设默认值，必须显式传入：
```bash
BOOK_DIR=storybook/xxx-0318 npx tsx generate.ts characters
BOOK_DIR=storybook/xxx-0318 npx tsx generate.ts pages
BOOK_DIR=storybook/xxx-0318 npx tsx stitch.ts
BOOK_DIR=storybook/xxx-0318 npx tsx video.ts
```

## 创作流程

用户输入格式：`/storybook {故事主题}`，例如 `/storybook 小蝌蚪找妈妈`

### 第一步：读取配置

1. 读取项目根目录的 `.env` 文件获取 API 配置
2. 读取 `prototype/pip-and-posy/pip-and-posy-story-pattern.md` 了解叙事模式参考

### 第二步：角色设计（characters）

基于故事主题，设计 2-4 个角色：

1. 为每个角色定义：
   - 名字（中英文）
   - 物种/类型
   - 外观特征（详细描述颜色、服装、体型等）
   - 性格特征
   - 角色关系

2. 将角色定义写入 `storybook/{slug}-{timestamp}/characters/characters.md`

3. 生成角色参考图（character sheet），调用图片生成 API：
   - Prompt 要求：白色背景、角色正面/侧面、简洁的儿童绘本风格（可参考Axel Scheffler《Pip and Posy》系列绘本风格）、一致的设计
   - 保存为 `storybook/{slug}-{timestamp}/characters/characters.jpeg`

### 第三步：分镜脚本（storyboard）

参考 Pip and Posy 的 "OH DEAR → HURRAY" 叙事结构，创作 8-12 页（不超过12页）的分镜脚本：

```
结构模板：
1. 日常场景建立（Setup）→ 两个好朋友在做某件平常的事
2. "OH DEAR" 时刻（Conflict）→ 幼儿真实会犯的小问题
3. 情绪低谷（Emotional Low）→ 角色感到伤心/尴尬/害怕
4. "HURRAY" 时刻（Resolution）→ 好朋友用行动帮忙解决
5. 温暖结尾（Warm Ending）→ 一起做开心的事
6. 故事核心主题提炼（Core Theme）→ 一句话点出核心主题，如：分享合作 —— 好东西和朋友轮流分享、一起玩，会比自己独占更快乐。
```

创作规则：
- **从不说教**：解决方式是朋友的善意和行动，不是大道理
- **不怪罪**：出了问题没人批评指责
- **情绪真实**：允许角色哭泣、生气、害怕
- **每页 1-3 句话**：用词简单，句式重复建立节奏感
- **画面独立叙事**：不依赖文字

每页图包含：
- 页码
- 文字内容（中文，1-3 句简短的话）
- 画面描述（详细描述场景、角色动作、表情、环境）

保存为 `storybook/{slug}-{timestamp}/storyboard.md`

### 第四步：生成 Prompts

为每一页（含封面）生成图片生成 Prompt：

1. **封面 Prompt**：包含书名、主角、风格要求
2. **内页 Prompt**：基于分镜脚本的画面描述，融合角色参考图的外观细节，每页画面的对应页码，每页画面的对应文字内容

Prompt 编写要求：
- 使用英文编写（图片生成模型效果更好）
- 明确指定画风：children's picture book illustration style, soft watercolor, warm and soft colors,  round shapes, no complex details, simple background，suitable for 0-5 years old kids"
- 明确描述角色外观（与角色参考图characters sheet一致）
- 描述场景、动作、表情、光线、氛围
- 添加 "consistent character design" 以保持角色一致性

分别保存为：
- `storybook/{slug}-{timestamp}/prompts/00-cover.md`
- `storybook/{slug}-{timestamp}/prompts/01-page.md`
- `storybook/{slug}-{timestamp}/prompts/02-page.md`
- ...

### 第五步：生成插画

使用 Gemini API 逐页生成插画：

**API 调用方式：**

```bash
curl "${AI_BASE_URL}/v1beta/models/${AI_IMAGE_MODEL}:generateContent" \
  -H "x-goog-api-key: ${AI_API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [{
      "parts": [
        {"text": "Generate an illustration: {prompt_content}"},
        {"inline_data": {"mime_type": "image/jpeg", "data": "{base64_of_character_reference}"}}
      ]
    }],
    "generationConfig": {
      "responseModalities": ["TEXT", "IMAGE"]
    }
  }'
```

**注意事项：**
- 每次生成时都附带角色参考图（character sheet）作为 inline_data，确保角色一致性
- 响应中图片数据在 `candidates[0].content.parts` 中，找到 `inlineData` 类型的 part
- 将 base64 数据解码后保存为 JPEG 文件
- 如果 API 调用失败，等待 5 秒后重试，最多重试 3 次
- base64_of_character_reference的内容是 character sheet 的 base64 数据
- 每次生成出的图将图片IMAGE和TEXT合并成一张图再输出保存

保存文件：
- `storybook/{slug}-{timestamp}/00-cover.jpeg`
- `storybook/{slug}-{timestamp}/01-page.jpeg`
- ...

### 第六步：生成长图预览

## 输出目录结构

```
storybook/{slug}-{timestamp}/
├── characters/
│   ├── characters.md       # 角色定义
│   └── characters.jpeg     # 角色参考图
├── storyboard.md           # 分镜脚本
├── prompts/
│   ├── 00-cover.md         # 封面 Prompt
│   └── 01-page.md ~ ...    # 各页 Prompt
├── 00-cover.jpeg ~ ...     # 页面插画(图文)
├── all-pages.jpeg          # 长图预览
└── storybook-{slug}.mp4    # 有声视频
```

## slug 和 timestamp 规则

- `slug`：故事主题的英文缩写（如"小蝌蚪找妈妈" → "tadpole-finds-mama"），使用小写字母和连字符
- `timestamp`：格式为 `MMdd`（月日），如 3 月 18 日 → `0318`

## 画风参考

- 风格：温暖的儿童绘本插画，类似 Axel Scheffler《Pip and Posy》系列绘本的画风
- 色调：明亮温暖，柔和的水彩质感
- 构图：简洁清晰，角色突出，背景简单
- 目标读者：0-5 岁幼儿